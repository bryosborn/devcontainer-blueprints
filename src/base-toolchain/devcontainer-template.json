{
  "id": "base-toolchain",
  "version": "0.2.0",
  "name": "Base Toolchain",
  "description": "Extends base-vscode with offline-installed APT packages, language toolchains, and selected CLI tools; the inherited VSIX archive remains uninstalled.",
  "options": {
    "baseImage": {
      "type": "string",
      "default": "devcontainers/base-vscode:0.2.0",
      "description": "Base image containing Docker-outside-of-Docker and the selected VS Code Server."
    }
  }
}
