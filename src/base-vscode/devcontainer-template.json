{
  "id": "base-vscode",
  "version": "0.2.0",
  "name": "Base VS Code Server",
  "description": "Build a dev container from the local DOD base image with a pinned VS Code Server payload baked in.",
  "documentationURL": "https://github.com/bryosborn/devcontainer-blueprints",
  "options": {
    "baseImage": {
      "type": "string",
      "default": "devcontainers/base-dod:0.2.0",
      "description": "Base image to extend. Build scripts produce devcontainers/base-dod:0.2.0 by default."
    },
    "vscodeCommit": {
      "type": "string",
      "default": "a44adf7f53e00964ab890f9f8758a334f1fc15bc",
      "description": "VS Code commit ID whose server archive should be baked into the image."
    },
    "vscodeQuality": {
      "type": "string",
      "default": "stable",
      "enum": [
        "stable",
        "insider"
      ],
      "description": "VS Code update channel used to fetch the server archive."
    },
    "vscodeServerPlatform": {
      "type": "string",
      "default": "server-linux-x64",
      "enum": [
        "server-linux-x64",
        "server-linux-arm64"
      ],
      "description": "Target platform of the VS Code Server archive."
    }
  }
}
