# Toolchain Artifacts

This directory is a local cache for downloaded toolchain archives.

The archive files and generated metadata are intentionally ignored by git. Recreate them from `config/toolchain.env` with the module prefetch scripts under `src/tool-artifacts/`.
