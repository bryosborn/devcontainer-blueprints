# Task: Add Offline-Capable VS Code Server Prefetch + Dockerfile Install Scripts

## Goal

Add scripts to this repository that can prefetch and install the correct Microsoft VS Code Server payload for a given VS Code version or commit.

The immediate target is **Linux devcontainers**, not Windows containers.

For Linux devcontainers, do **not** look for `vscode-server.exe`.

The artifact to install is a compressed archive such as:

```text
vscode-server-linux-x64.tar.gz
vscode-server-linux-arm64.tar.gz
```

The correct VS Code Server is selected by the **VS Code client commit SHA**, not merely by the human version number.

Example:

```text
VS Code version: 1.124.2
VS Code commit:  <40-character commit SHA>
Server platform: server-linux-x64
Quality:         stable
```

The script should support resolving a VS Code version to its matching commit SHA and then downloading the matching VS Code Server archive.

---

# Important Behavior

VS Code normally installs and manages VS Code Server automatically when the client connects to a remote, WSL environment, SSH host, or devcontainer.

This task intentionally preinstalls VS Code Server into the image so that a devcontainer can start faster or work in an offline environment.

The preinstalled server must match the VS Code client commit exactly.

If VS Code updates locally, the container image may need to be rebuilt with the new server commit.

---

# Supported Initial Scope

Implement support for:

```text
quality: stable
remote OS: Linux glibc-based container
architectures:
  - x64
  - arm64
```

Do not implement Windows containers yet.

Do not implement Alpine/musl containers yet.

Do not install VS Code extensions yet. Extension caching is a separate problem and should be handled later with VSIX artifacts.

---

# Required Repository Changes

Add these files:

```text
config/
└── vscode-server.env

scripts/
├── prefetch-vscode-server.sh
├── install-vscode-server.sh
└── test-vscode-server-install.sh

test/
└── Dockerfile.vscode-server
```

Also update `.gitignore` to ignore downloaded VS Code Server archives by default.

---

# Directory Layout for Downloaded Artifacts

Downloaded VS Code Server artifacts should be placed under:

```text
artifacts/vscode-server/
└── stable/
    └── <commit>/
        └── server-linux-x64/
            ├── vscode-server-linux-x64.tar.gz
            ├── metadata.json
            └── SHA256SUMS
```

For ARM64:

```text
artifacts/vscode-server/
└── stable/
    └── <commit>/
        └── server-linux-arm64/
            ├── vscode-server-linux-arm64.tar.gz
            ├── metadata.json
            └── SHA256SUMS
```

The prefetch script should also write a convenience pointer:

```text
artifacts/vscode-server/current-stable-server-linux-x64.json
```

or:

```text
artifacts/vscode-server/current-stable-server-linux-arm64.json
```

---

# Add `.gitignore` Entries

Append:

```gitignore
# VS Code Server downloaded artifacts
artifacts/vscode-server/**/*.tar.gz
artifacts/vscode-server/**/*.zip
artifacts/vscode-server/**/SHA256SUMS
artifacts/vscode-server/current-*.json
```

Keep `metadata.json` tracked only if desired. For this first version, it is fine if metadata is ignored or tracked; prefer ignoring large binary archives.

---

# Create `config/vscode-server.env`

```bash
# Default VS Code Server prefetch config.

# Use an exact version like 1.124.2, or "latest".
VSCODE_VERSION=latest

# Initial implementation supports stable only.
VSCODE_QUALITY=stable

# The platform used to resolve version metadata.
# For Linux x64 hosts, use linux-x64.
# For Linux ARM64 hosts, use linux-arm64.
VSCODE_CLIENT_PLATFORM=linux-x64

# The VS Code Server platform to download.
# For x64 Linux containers, use server-linux-x64.
# For ARM64 Linux containers, use server-linux-arm64.
VSCODE_SERVER_PLATFORM=server-linux-x64

# The user whose home should receive ~/.vscode-server during Docker build.
VSCODE_REMOTE_USER=vscode

# Artifact root relative to repository root.
VSCODE_SERVER_ARTIFACT_ROOT=artifacts/vscode-server
```

---

# Script 1: `scripts/prefetch-vscode-server.sh`

## Purpose

This script runs during the online prefetch phase.

It should:

1. Accept either a VS Code version or a commit SHA.
2. Resolve version to commit SHA when needed.
3. Download the matching VS Code Server archive.
4. Verify SHA256 when metadata provides it.
5. Write metadata for later Dockerfile use.
6. Avoid downloading again if the archive already exists and passes validation.

## Required CLI

Support:

```bash
./scripts/prefetch-vscode-server.sh
```

```bash
./scripts/prefetch-vscode-server.sh --version 1.124.2
```

```bash
./scripts/prefetch-vscode-server.sh --version latest
```

```bash
./scripts/prefetch-vscode-server.sh --commit <commit-sha>
```

```bash
./scripts/prefetch-vscode-server.sh \
  --version 1.124.2 \
  --quality stable \
  --client-platform linux-x64 \
  --server-platform server-linux-x64
```

## Required Implementation

Create the file with this implementation:

```bash
#!/usr/bin/env bash
set -euo pipefail

usage() {
  cat <<'USAGE'
Usage:
  prefetch-vscode-server.sh [options]

Options:
  --version VERSION          VS Code product version, e.g. 1.124.2 or latest.
  --commit COMMIT           Exact VS Code commit SHA. If set, version resolution is skipped.
  --quality QUALITY         VS Code quality. Initial supported value: stable.
  --client-platform PLATFORM
                             Metadata platform, e.g. linux-x64 or linux-arm64.
  --server-platform PLATFORM
                             Server platform, e.g. server-linux-x64 or server-linux-arm64.
  --artifact-root DIR       Artifact root. Default: artifacts/vscode-server.
  -h, --help                Show help.

Examples:
  ./scripts/prefetch-vscode-server.sh --version 1.124.2
  ./scripts/prefetch-vscode-server.sh --commit abcdef123456...
USAGE
}

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

# Defaults may be overridden by config/vscode-server.env or flags.
VSCODE_VERSION="latest"
VSCODE_COMMIT=""
VSCODE_QUALITY="stable"
VSCODE_CLIENT_PLATFORM="linux-x64"
VSCODE_SERVER_PLATFORM="server-linux-x64"
VSCODE_SERVER_ARTIFACT_ROOT="artifacts/vscode-server"

if [ -f "${REPO_ROOT}/config/vscode-server.env" ]; then
  # shellcheck source=/dev/null
  source "${REPO_ROOT}/config/vscode-server.env"
fi

while [ "$#" -gt 0 ]; do
  case "$1" in
    --version)
      VSCODE_VERSION="$2"
      shift 2
      ;;
    --commit)
      VSCODE_COMMIT="$2"
      shift 2
      ;;
    --quality)
      VSCODE_QUALITY="$2"
      shift 2
      ;;
    --client-platform)
      VSCODE_CLIENT_PLATFORM="$2"
      shift 2
      ;;
    --server-platform)
      VSCODE_SERVER_PLATFORM="$2"
      shift 2
      ;;
    --artifact-root)
      VSCODE_SERVER_ARTIFACT_ROOT="$2"
      shift 2
      ;;
    -h|--help)
      usage
      exit 0
      ;;
    *)
      echo "ERROR: unknown argument: $1" >&2
      usage
      exit 1
      ;;
  esac
done

if [ "${VSCODE_QUALITY}" != "stable" ]; then
  echo "ERROR: initial implementation supports only --quality stable." >&2
  exit 1
fi

case "${VSCODE_SERVER_PLATFORM}" in
  server-linux-x64|server-linux-arm64)
    ;;
  *)
    echo "ERROR: initial implementation supports only server-linux-x64 and server-linux-arm64." >&2
    exit 1
    ;;
esac

for cmd in curl jq sha256sum tar; do
  if ! command -v "${cmd}" >/dev/null 2>&1; then
    echo "ERROR: required command not found: ${cmd}" >&2
    exit 1
  fi
done

ARTIFACT_ROOT_ABS="${REPO_ROOT}/${VSCODE_SERVER_ARTIFACT_ROOT}"

json_field_or_empty() {
  local json="$1"
  local field="$2"
  jq -r "${field} // empty" <<<"${json}"
}

resolve_commit_from_version() {
  local version="$1"
  local quality="$2"
  local client_platform="$3"
  local metadata_url
  local metadata

  if [ "${version}" = "latest" ]; then
    metadata_url="https://update.code.visualstudio.com/api/update/${client_platform}/${quality}/latest"
  else
    metadata_url="https://update.code.visualstudio.com/api/versions/${version}/${client_platform}/${quality}"
  fi

  echo "Resolving VS Code metadata:"
  echo "  ${metadata_url}" >&2

  metadata="$(curl --fail --silent --show-error --location "${metadata_url}")"

  local commit
  commit="$(json_field_or_empty "${metadata}" '.version')"

  if [ -z "${commit}" ] || [ "${commit}" = "null" ]; then
    echo "ERROR: could not resolve commit from metadata." >&2
    echo "${metadata}" >&2
    exit 1
  fi

  echo "${metadata}"
}

download_with_retries() {
  local url="$1"
  local output="$2"

  curl \
    --fail \
    --location \
    --retry 5 \
    --retry-delay 2 \
    --connect-timeout 20 \
    --show-error \
    --output "${output}" \
    "${url}"
}

PRODUCT_VERSION="${VSCODE_VERSION}"
RESOLVED_METADATA=""

if [ -z "${VSCODE_COMMIT}" ]; then
  RESOLVED_METADATA="$(resolve_commit_from_version "${VSCODE_VERSION}" "${VSCODE_QUALITY}" "${VSCODE_CLIENT_PLATFORM}")"
  VSCODE_COMMIT="$(json_field_or_empty "${RESOLVED_METADATA}" '.version')"
  PRODUCT_VERSION="$(json_field_or_empty "${RESOLVED_METADATA}" '.productVersion')"
  if [ -z "${PRODUCT_VERSION}" ]; then
    PRODUCT_VERSION="$(json_field_or_empty "${RESOLVED_METADATA}" '.name')"
  fi
  if [ -z "${PRODUCT_VERSION}" ]; then
    PRODUCT_VERSION="${VSCODE_VERSION}"
  fi
else
  echo "Using explicit VS Code commit:"
  echo "  ${VSCODE_COMMIT}"
fi

if ! [[ "${VSCODE_COMMIT}" =~ ^[0-9a-f]{40}$ ]]; then
  echo "ERROR: commit does not look like a 40-character SHA: ${VSCODE_COMMIT}" >&2
  exit 1
fi

SERVER_METADATA_URL="https://update.code.visualstudio.com/api/versions/commit:${VSCODE_COMMIT}/${VSCODE_SERVER_PLATFORM}/${VSCODE_QUALITY}"

echo "Resolving VS Code Server metadata:"
echo "  ${SERVER_METADATA_URL}"

SERVER_METADATA=""
if SERVER_METADATA="$(curl --fail --silent --show-error --location "${SERVER_METADATA_URL}")"; then
  SERVER_URL="$(json_field_or_empty "${SERVER_METADATA}" '.url')"
  SERVER_SHA256="$(json_field_or_empty "${SERVER_METADATA}" '.sha256hash')"
else
  echo "WARNING: server metadata endpoint failed; falling back to constructed download URL." >&2
  SERVER_URL=""
  SERVER_SHA256=""
fi

if [ -z "${SERVER_URL}" ]; then
  SERVER_URL="https://update.code.visualstudio.com/commit:${VSCODE_COMMIT}/${VSCODE_SERVER_PLATFORM}/${VSCODE_QUALITY}"
fi

SERVER_SUFFIX="${VSCODE_SERVER_PLATFORM#server-}"
ARCHIVE_NAME="vscode-server-${SERVER_SUFFIX}.tar.gz"

DEST_DIR="${ARTIFACT_ROOT_ABS}/${VSCODE_QUALITY}/${VSCODE_COMMIT}/${VSCODE_SERVER_PLATFORM}"
ARCHIVE_PATH="${DEST_DIR}/${ARCHIVE_NAME}"
METADATA_PATH="${DEST_DIR}/metadata.json"
SHA_PATH="${DEST_DIR}/SHA256SUMS"
CURRENT_POINTER="${ARTIFACT_ROOT_ABS}/current-${VSCODE_QUALITY}-${VSCODE_SERVER_PLATFORM}.json"

mkdir -p "${DEST_DIR}"

NEEDS_DOWNLOAD=1

if [ -f "${ARCHIVE_PATH}" ]; then
  if [ -n "${SERVER_SHA256}" ]; then
    if echo "${SERVER_SHA256}  ${ARCHIVE_PATH}" | sha256sum --check --status; then
      NEEDS_DOWNLOAD=0
      echo "Archive already exists and SHA256 matches:"
      echo "  ${ARCHIVE_PATH}"
    fi
  else
    NEEDS_DOWNLOAD=0
    echo "Archive already exists; no upstream SHA256 available to verify:"
    echo "  ${ARCHIVE_PATH}"
  fi
fi

if [ "${NEEDS_DOWNLOAD}" -eq 1 ]; then
  TMP_PATH="${ARCHIVE_PATH}.tmp"
  rm -f "${TMP_PATH}"

  echo "Downloading VS Code Server:"
  echo "  ${SERVER_URL}"
  echo "to:"
  echo "  ${ARCHIVE_PATH}"

  download_with_retries "${SERVER_URL}" "${TMP_PATH}"

  if [ -n "${SERVER_SHA256}" ]; then
    echo "${SERVER_SHA256}  ${TMP_PATH}" | sha256sum --check
  fi

  mv "${TMP_PATH}" "${ARCHIVE_PATH}"
fi

ACTUAL_SHA256="$(sha256sum "${ARCHIVE_PATH}" | awk '{print $1}')"

cat > "${SHA_PATH}" <<EOF
${ACTUAL_SHA256}  ${ARCHIVE_NAME}
EOF

jq -n \
  --arg productVersion "${PRODUCT_VERSION}" \
  --arg commit "${VSCODE_COMMIT}" \
  --arg quality "${VSCODE_QUALITY}" \
  --arg clientPlatform "${VSCODE_CLIENT_PLATFORM}" \
  --arg serverPlatform "${VSCODE_SERVER_PLATFORM}" \
  --arg url "${SERVER_URL}" \
  --arg archive "${ARCHIVE_PATH}" \
  --arg archiveName "${ARCHIVE_NAME}" \
  --arg sha256 "${ACTUAL_SHA256}" \
  --arg downloadedAt "$(date -u +'%Y-%m-%dT%H:%M:%SZ')" \
  '{
    productVersion: $productVersion,
    commit: $commit,
    quality: $quality,
    clientPlatform: $clientPlatform,
    serverPlatform: $serverPlatform,
    url: $url,
    archive: $archive,
    archiveName: $archiveName,
    sha256: $sha256,
    downloadedAt: $downloadedAt
  }' > "${METADATA_PATH}"

cp "${METADATA_PATH}" "${CURRENT_POINTER}"

echo "Prefetch complete."
echo "  commit:   ${VSCODE_COMMIT}"
echo "  archive:  ${ARCHIVE_PATH}"
echo "  metadata: ${METADATA_PATH}"
echo "  current:  ${CURRENT_POINTER}"
```

---

# Script 2: `scripts/install-vscode-server.sh`

## Purpose

This script runs during the Docker build.

It must install the already-downloaded VS Code Server archive into the remote user's home directory.

It must not use the internet.

It should install both known layouts:

```text
Current/newer layout:
~/.vscode-server/cli/servers/Stable-<commit>/server

Legacy layout:
~/.vscode-server/bin/<commit>
```

For the legacy layout, also create:

```text
~/.vscode-server/bin/<commit>/0
```

## Required CLI

Support:

```bash
install-vscode-server.sh \
  --commit <commit> \
  --archive /path/to/vscode-server-linux-x64.tar.gz \
  --user vscode
```

Also support:

```bash
install-vscode-server.sh \
  --commit <commit> \
  --archive /path/to/vscode-server-linux-x64.tar.gz \
  --server-home /home/vscode
```

## Required Implementation

Create the file:

```bash
#!/usr/bin/env bash
set -euo pipefail

usage() {
  cat <<'USAGE'
Usage:
  install-vscode-server.sh --commit COMMIT --archive ARCHIVE [options]

Options:
  --commit COMMIT       Required. VS Code commit SHA.
  --archive ARCHIVE     Required. Path to vscode-server-linux-*.tar.gz.
  --user USER           Remote user. Default: vscode.
  --server-home DIR     Explicit home directory. If omitted, resolved from passwd.
  --quality QUALITY     Initial supported value: stable.
  --no-legacy-layout    Do not install ~/.vscode-server/bin/<commit>.
  -h, --help            Show help.
USAGE
}

COMMIT=""
ARCHIVE=""
REMOTE_USER="vscode"
SERVER_HOME=""
QUALITY="stable"
INSTALL_LEGACY=1

while [ "$#" -gt 0 ]; do
  case "$1" in
    --commit)
      COMMIT="$2"
      shift 2
      ;;
    --archive)
      ARCHIVE="$2"
      shift 2
      ;;
    --user)
      REMOTE_USER="$2"
      shift 2
      ;;
    --server-home)
      SERVER_HOME="$2"
      shift 2
      ;;
    --quality)
      QUALITY="$2"
      shift 2
      ;;
    --no-legacy-layout)
      INSTALL_LEGACY=0
      shift
      ;;
    -h|--help)
      usage
      exit 0
      ;;
    *)
      echo "ERROR: unknown argument: $1" >&2
      usage
      exit 1
      ;;
  esac
done

if [ -z "${COMMIT}" ]; then
  echo "ERROR: --commit is required." >&2
  usage
  exit 1
fi

if [ -z "${ARCHIVE}" ]; then
  echo "ERROR: --archive is required." >&2
  usage
  exit 1
fi

if [ "${QUALITY}" != "stable" ]; then
  echo "ERROR: initial implementation supports only --quality stable." >&2
  exit 1
fi

if ! [[ "${COMMIT}" =~ ^[0-9a-f]{40}$ ]]; then
  echo "ERROR: commit does not look like a 40-character SHA: ${COMMIT}" >&2
  exit 1
fi

if [ ! -f "${ARCHIVE}" ]; then
  echo "ERROR: archive does not exist: ${ARCHIVE}" >&2
  exit 1
fi

for cmd in tar stat; do
  if ! command -v "${cmd}" >/dev/null 2>&1; then
    echo "ERROR: required command not found: ${cmd}" >&2
    exit 1
  fi
done

if [ -z "${SERVER_HOME}" ]; then
  if ! getent passwd "${REMOTE_USER}" >/dev/null 2>&1; then
    echo "ERROR: user not found and --server-home was not provided: ${REMOTE_USER}" >&2
    exit 1
  fi
  SERVER_HOME="$(getent passwd "${REMOTE_USER}" | cut -d: -f6)"
fi

if [ -z "${SERVER_HOME}" ] || [ ! -d "${SERVER_HOME}" ]; then
  echo "ERROR: server home does not exist: ${SERVER_HOME}" >&2
  exit 1
fi

VSCODE_ROOT="${SERVER_HOME}/.vscode-server"

CURRENT_DIR="${VSCODE_ROOT}/cli/servers/Stable-${COMMIT}/server"
LEGACY_DIR="${VSCODE_ROOT}/bin/${COMMIT}"

install_archive_to_dir() {
  local target_dir="$1"
  local staging_dir="${target_dir}.staging.$$"

  rm -rf "${staging_dir}"
  mkdir -p "${staging_dir}"

  tar -xzf "${ARCHIVE}" -C "${staging_dir}" --strip-components=1

  if [ ! -e "${staging_dir}/bin/code-server" ]; then
    echo "ERROR: extracted archive does not contain bin/code-server." >&2
    echo "Archive may not be a VS Code Server Linux archive." >&2
    find "${staging_dir}" -maxdepth 3 -type f | sort | head -100 >&2
    exit 1
  fi

  mkdir -p "$(dirname "${target_dir}")"
  rm -rf "${target_dir}"
  mv "${staging_dir}" "${target_dir}"
}

echo "Installing VS Code Server for commit:"
echo "  ${COMMIT}"
echo "Archive:"
echo "  ${ARCHIVE}"
echo "Home:"
echo "  ${SERVER_HOME}"

echo "Installing current layout:"
echo "  ${CURRENT_DIR}"
install_archive_to_dir "${CURRENT_DIR}"

if [ "${INSTALL_LEGACY}" -eq 1 ]; then
  echo "Installing legacy layout:"
  echo "  ${LEGACY_DIR}"
  install_archive_to_dir "${LEGACY_DIR}"
  touch "${LEGACY_DIR}/0"
fi

if command -v chown >/dev/null 2>&1 && id "${REMOTE_USER}" >/dev/null 2>&1; then
  echo "Setting ownership of ${VSCODE_ROOT} to ${REMOTE_USER}."
  chown -R "${REMOTE_USER}:${REMOTE_USER}" "${VSCODE_ROOT}" 2>/dev/null \
    || chown -R "${REMOTE_USER}" "${VSCODE_ROOT}"
fi

echo "Validating code-server binary."

if ! "${CURRENT_DIR}/bin/code-server" --version >/tmp/vscode-server-version.txt 2>&1; then
  echo "ERROR: code-server binary failed to run." >&2
  cat /tmp/vscode-server-version.txt >&2 || true
  exit 1
fi

cat /tmp/vscode-server-version.txt

echo "VS Code Server install complete."
```

---

# Test Dockerfile: `test/Dockerfile.vscode-server`

Create:

```dockerfile
FROM mcr.microsoft.com/devcontainers/base:ubuntu-24.04

ARG VSCODE_COMMIT
ARG VSCODE_QUALITY=stable
ARG VSCODE_SERVER_PLATFORM=server-linux-x64
ARG VSCODE_REMOTE_USER=vscode

USER root

COPY scripts/install-vscode-server.sh /usr/local/bin/install-vscode-server.sh
COPY artifacts/vscode-server/ /opt/vscode-server-artifacts/

RUN chmod +x /usr/local/bin/install-vscode-server.sh

RUN test -n "${VSCODE_COMMIT}" \
    && /usr/local/bin/install-vscode-server.sh \
        --commit "${VSCODE_COMMIT}" \
        --quality "${VSCODE_QUALITY}" \
        --user "${VSCODE_REMOTE_USER}" \
        --archive "/opt/vscode-server-artifacts/${VSCODE_QUALITY}/${VSCODE_COMMIT}/${VSCODE_SERVER_PLATFORM}/vscode-server-${VSCODE_SERVER_PLATFORM#server-}.tar.gz" \
    && rm -rf /opt/vscode-server-artifacts

USER vscode

WORKDIR /workspaces
```

---

# Script 3: `scripts/test-vscode-server-install.sh`

## Purpose

This script validates that:

1. A VS Code Server artifact has been prefetched.
2. A Docker image can install it with `--network=none`.
3. The expected files exist in both current and legacy layouts.
4. `bin/code-server --version` runs inside the image.

## Required Implementation

Create:

```bash
#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

QUALITY="${VSCODE_QUALITY:-stable}"
SERVER_PLATFORM="${VSCODE_SERVER_PLATFORM:-server-linux-x64}"
CURRENT_POINTER="${REPO_ROOT}/artifacts/vscode-server/current-${QUALITY}-${SERVER_PLATFORM}.json"

if [ ! -f "${CURRENT_POINTER}" ]; then
  echo "ERROR: current metadata not found:"
  echo "  ${CURRENT_POINTER}"
  echo "Run ./scripts/prefetch-vscode-server.sh first."
  exit 1
fi

if ! command -v jq >/dev/null 2>&1; then
  echo "ERROR: jq is required."
  exit 1
fi

COMMIT="$(jq -r '.commit' "${CURRENT_POINTER}")"

if [ -z "${COMMIT}" ] || [ "${COMMIT}" = "null" ]; then
  echo "ERROR: could not read commit from ${CURRENT_POINTER}"
  exit 1
fi

IMAGE_TAG="vscode-server-preinstall-test:${COMMIT}"

echo "Building offline test image:"
echo "  ${IMAGE_TAG}"

docker build \
  --network=none \
  -f "${REPO_ROOT}/test/Dockerfile.vscode-server" \
  --build-arg "VSCODE_COMMIT=${COMMIT}" \
  --build-arg "VSCODE_QUALITY=${QUALITY}" \
  --build-arg "VSCODE_SERVER_PLATFORM=${SERVER_PLATFORM}" \
  -t "${IMAGE_TAG}" \
  "${REPO_ROOT}"

echo "Running validation inside image."

docker run --rm "${IMAGE_TAG}" bash -lc "
  set -euo pipefail

  CURRENT_DIR=\"/home/vscode/.vscode-server/cli/servers/Stable-${COMMIT}/server\"
  LEGACY_DIR=\"/home/vscode/.vscode-server/bin/${COMMIT}\"

  test -x \"\${CURRENT_DIR}/bin/code-server\"
  test -x \"\${LEGACY_DIR}/bin/code-server\"
  test -f \"\${LEGACY_DIR}/0\"

  echo 'Current layout:'
  ls -la \"\${CURRENT_DIR}/bin/code-server\"

  echo 'Legacy layout:'
  ls -la \"\${LEGACY_DIR}/bin/code-server\"
  ls -la \"\${LEGACY_DIR}/0\"

  echo 'code-server version:'
  \"\${CURRENT_DIR}/bin/code-server\" --version
"

echo "VS Code Server preinstall test completed successfully."
```

---

# Make Scripts Executable

Run:

```bash
chmod +x scripts/prefetch-vscode-server.sh
chmod +x scripts/install-vscode-server.sh
chmod +x scripts/test-vscode-server-install.sh
```

---

# Online Prefetch Test

From the repo root, run:

```bash
./scripts/prefetch-vscode-server.sh --version latest
```

Or, for a pinned VS Code version:

```bash
./scripts/prefetch-vscode-server.sh --version 1.124.2
```

Then inspect:

```bash
cat artifacts/vscode-server/current-stable-server-linux-x64.json | jq .
```

Expected fields:

```json
{
  "productVersion": "...",
  "commit": "...",
  "quality": "stable",
  "clientPlatform": "linux-x64",
  "serverPlatform": "server-linux-x64",
  "url": "...",
  "archive": "...",
  "archiveName": "vscode-server-linux-x64.tar.gz",
  "sha256": "...",
  "downloadedAt": "..."
}
```

---

# Offline Docker Build Test

After prefetching, run:

```bash
./scripts/test-vscode-server-install.sh
```

This must build with:

```bash
docker build --network=none
```

The build should succeed without internet access.

---

# Example Use in a Real Devcontainer Dockerfile

In any image where you want the VS Code Server preinstalled, use the same pattern.

Example:

```dockerfile
FROM mcr.microsoft.com/devcontainers/base:ubuntu-24.04

ARG VSCODE_COMMIT
ARG VSCODE_QUALITY=stable
ARG VSCODE_SERVER_PLATFORM=server-linux-x64

USER root

COPY scripts/install-vscode-server.sh /usr/local/bin/install-vscode-server.sh
COPY artifacts/vscode-server/ /opt/vscode-server-artifacts/

RUN chmod +x /usr/local/bin/install-vscode-server.sh \
    && /usr/local/bin/install-vscode-server.sh \
        --commit "${VSCODE_COMMIT}" \
        --quality "${VSCODE_QUALITY}" \
        --user vscode \
        --archive "/opt/vscode-server-artifacts/${VSCODE_QUALITY}/${VSCODE_COMMIT}/${VSCODE_SERVER_PLATFORM}/vscode-server-${VSCODE_SERVER_PLATFORM#server-}.tar.gz" \
    && rm -rf /opt/vscode-server-artifacts

USER vscode

WORKDIR /workspaces
```

Build:

```bash
COMMIT="$(jq -r '.commit' artifacts/vscode-server/current-stable-server-linux-x64.json)"

docker build \
  --network=none \
  --build-arg "VSCODE_COMMIT=${COMMIT}" \
  -t local/devcontainer-with-vscode-server:${COMMIT} \
  .
```

---

# Manual VS Code Validation

After building the image, create a scratch devcontainer that uses it:

```jsonc
{
  "name": "VS Code Server Preinstall Test",
  "image": "local/devcontainer-with-vscode-server:<commit>",
  "remoteUser": "vscode"
}
```

Open the folder in VS Code using "Reopen in Container".

Then inspect the Dev Containers log.

Expected behavior:

```text
The container should not need to download the VS Code Server for that same commit.
```

If VS Code still downloads the server, check these likely causes:

1. The local VS Code client has a different commit than the one baked into the image.
2. The remote user is not `vscode`, so the server was installed into the wrong home directory.
3. The devcontainer config mounts a volume over `/home/vscode` or `/home/vscode/.vscode-server`.
4. VS Code changed the expected server layout again.
5. The image was built for the wrong architecture.
6. The container is Alpine/musl-based, but the script downloaded the glibc Linux server.

To find the commit of the local VS Code client, run on the host:

```bash
code --version
```

The output is usually:

```text
<product version>
<commit sha>
<architecture>
```

Use the second line as the exact commit:

```bash
VSCODE_COMMIT="$(code --version | sed -n '2p')"

./scripts/prefetch-vscode-server.sh --commit "${VSCODE_COMMIT}"
```

Then rebuild the image.

---

# Notes on Version vs Commit

The VS Code version number is not the server identity.

The server identity is the commit SHA.

Good:

```bash
./scripts/prefetch-vscode-server.sh --commit "$(code --version | sed -n '2p')"
```

Acceptable:

```bash
./scripts/prefetch-vscode-server.sh --version 1.124.2
```

Riskier:

```bash
./scripts/prefetch-vscode-server.sh --version latest
```

Using `latest` is convenient for development, but it is not reproducible. Prefer exact versions or exact commits for offline-capable images.

---

# Architecture Mapping

Initial mapping:

```text
x86_64 / amd64     -> server-linux-x64
aarch64 / arm64    -> server-linux-arm64
```

Do not add 32-bit ARM support in this first version.

Do not add Windows support in this first version.

If Windows container support is needed later, it should be a separate implementation because the artifact is a zip archive and uses a different server platform name.

---

# Do Not Do These Things

Do not use `latest` inside the Dockerfile.

Do not run `curl` from the Dockerfile to fetch VS Code Server.

Do not install the server into `/root/.vscode-server` unless the devcontainer actually uses `root` as `remoteUser`.

Do not assume the version number alone is enough.

Do not mount a volume over the remote user's `.vscode-server` directory if the goal is to use the baked-in server.

Do not install extensions in this script yet.

Do not support Cursor or other VS Code forks in this first implementation. Their commit IDs may not exist on Microsoft’s VS Code update server.

---

# Acceptance Criteria

The implementation is complete when all of these pass:

```bash
./scripts/prefetch-vscode-server.sh --version latest
./scripts/test-vscode-server-install.sh
```

The test must build with:

```bash
docker build --network=none
```

The test container must contain:

```text
/home/vscode/.vscode-server/cli/servers/Stable-<commit>/server/bin/code-server
/home/vscode/.vscode-server/bin/<commit>/bin/code-server
/home/vscode/.vscode-server/bin/<commit>/0
```

And this must run successfully inside the test container:

```bash
/home/vscode/.vscode-server/cli/servers/Stable-<commit>/server/bin/code-server --version
```

---

# Future Enhancements

Do not implement these now, but keep the code clean enough to add them later:

1. Cache VS Code CLI artifacts.
2. Cache VSIX extension files.
3. Support ARM64 automatically from `docker buildx` target platform.
4. Support Alpine/musl containers.
5. Support Windows containers.
6. Mirror VS Code Server artifacts into Nexus or another internal artifact repository.
7. Add a manifest lock file so images are rebuilt from pinned VS Code commit and SHA256 only.
8. Add a script that reads the local host's `code --version` automatically and prefetches the exact matching commit.

---

# Final Implementation Rule

The Docker build must be offline.

The prefetch script may use the internet.

The install script and Dockerfile must use only files already present under:

```text
artifacts/vscode-server/
```

The resulting image should be usable by VS Code without downloading the same VS Code Server payload again, as long as the local VS Code client commit matches the preinstalled commit.